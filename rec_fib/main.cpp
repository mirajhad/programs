#include <iostream>
#include<conio.h>

using namespace std;
main()
{
    int i;
    for(i=1;i<=10;i++)
    printf("the fibonacci series are %d",fib(i));
    if(n==1||n==2)
        return(1);
    return(fib(n-1)+fib(n-2));
    getch();
}

